I now want AI agents to be able to receive tasks too, in addition to human team members.

An AI agent has a provider name and a model name. When an AI agent is assigned a task, the application should be able to ask that agent to execute the task and store an execution result.

For this project, do not call a real external AI service. Use a simple testable local/fake implementation so the behavior can be demonstrated without network access.

Human assignees should continue to work as before.
