# Goals

<!-- The product's intent, as durable facts — not a discussion, not history, not a session-recovery
     log. Insights live here integrally: a goal-level decision and its reason belong next to the goal,
     not in a separate "insights" section. Keep it true: a goal that changed is edited or removed,
     never left stale — a formal doc that lies with authority is worse than none. Where a fact is
     already enforced in code, point to it rather than restating it. -->

## Primary goal

<!-- One or two sentences: what this product is for. -->

## Use scenarios

<!-- Only the start-to-useful-result scenarios that materially shape engineering decisions. -->

## Non-goals

<!-- What we deliberately do not design for (yet), so the Worker does not build for it. -->

## Decisions & insights

<!-- Goal-level facts with rationale: "the primary goal is X, not Y, because Z"; a product rule that
     must always hold; an observable trade-off the user chose. Fact + reason, not the deliberation. -->
